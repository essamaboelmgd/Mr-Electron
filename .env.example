# This root file is a reference map only.
# The API and each Vite app read environment variables from their own folder.
# Copy the matching file to .env (local) or .env.production.local (static production build).

# API: api/.env.example
# Student: student/.env.example
# Admin: admin/.env.example
# Landing: landingpage/.env.example
