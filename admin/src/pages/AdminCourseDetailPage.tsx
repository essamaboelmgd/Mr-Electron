import { FormEvent, useEffect, useState } from 'react';
import { ArrowRight, Check, ChevronLeft, Clock3, Edit3, ExternalLink, FileText, Link2, Plus, RefreshCw, Trash2, Video, X } from 'lucide-react';
import { useNavigate, useParams } from 'react-router-dom';
import { AppShell } from '@/components/AppShell';
import { Course, createLesson, deleteLesson, getCourseById, getCourseExams, getCourseLessons, Lesson, updateLesson } from '@/services/coursesService';

const blankLesson = { title: '', duration: '0', description: '', order: '0', videoUrl: '' };
export default function AdminCourseDetailPage() {
  const { id } = useParams<{ id: string }>(); const navigate = useNavigate(); const [course, setCourse] = useState<Course | null>(null); const [lessons, setLessons] = useState<Lesson[]>([]); const [exams, setExams] = useState<any[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [dialog, setDialog] = useState(false); const [editing, setEditing] = useState<Lesson | null>(null); const [form, setForm] = useState(blankLesson); const [saving, setSaving] = useState(false);
  const load = () => { if (!id) return; setLoading(true); Promise.all([getCourseById(id), getCourseLessons(id), getCourseExams(id)]).then(([chapter, chapterLessons, chapterExams]) => { setCourse(chapter); setLessons(chapterLessons); setExams(chapterExams); }).catch((requestError: any) => setError(requestError.response?.data?.message || 'تعذر تحميل الباب.')).finally(() => setLoading(false)); };
  useEffect(load, [id]);
  const openCreate = () => { setEditing(null); setForm({ ...blankLesson, order: String(lessons.length + 1) }); setDialog(true); setError(''); };
  const openEdit = (lesson: Lesson) => { setEditing(lesson); setForm({ title: lesson.title, duration: String(lesson.duration || 0), description: lesson.description || '', order: String(lesson.order || 0), videoUrl: lesson.videoUrl || '' }); setDialog(true); setError(''); };
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!id || !form.title.trim() || !form.videoUrl.trim()) { setError('اسم الدرس ورابط YouTube أو Vimeo مطلوبان.'); return; } setSaving(true); setError(''); try { const payload = { title: form.title.trim(), duration: Number(form.duration) || 0, description: form.description.trim(), order: Number(form.order) || 0, videoUrl: form.videoUrl.trim() }; if (editing) await updateLesson(editing._id, payload); else await createLesson({ courseId: id, ...payload }); setDialog(false); load(); } catch (requestError: any) { setError(requestError.response?.data?.message || 'تعذر حفظ الدرس. تأكد من رابط الفيديو.'); } finally { setSaving(false); } };
  const removeLesson = async (lesson: Lesson) => { if (!window.confirm(`حذف الدرس «${lesson.title}»؟`)) return; try { await deleteLesson(lesson._id); load(); } catch { setError('تعذر حذف الدرس.'); } };
  if (loading) return <AppShell><div className="admin-loading-panel"><span className="spinner" /> جارٍ تحميل الباب...</div></AppShell>;
  if (!course) return <AppShell><div className="admin-empty"><RefreshCw size={24} /><h2>الباب غير موجود</h2><button type="button" className="admin-secondary" onClick={() => navigate('/curriculum')}>العودة للمنهج</button></div></AppShell>;
  return <AppShell><div className="admin-page-stack"><button className="admin-back-link" type="button" onClick={() => navigate('/curriculum')}><ArrowRight size={17} /> العودة للأبواب</button><section className="admin-chapter-hero"><div><span className="admin-eyebrow">{course.term === 'first' ? 'الترم الأول' : 'الترم الثاني'} · {course.educationalLevel?.nameAr}</span><h1>{course.title}</h1><p>{course.description || 'أضف وصفًا يساعد الطالب على معرفة محتوى الباب.'}</p></div><span className={course.isActive ? 'admin-status open large-status' : 'admin-status closed large-status'}>{course.isActive ? <><Check size={14} /> منشور</> : 'مخفي'}</span></section>{error && <div className="admin-error" role="alert">{error}<button type="button" onClick={() => { setError(''); load(); }}><RefreshCw size={14} /> إعادة المحاولة</button></div>}<div className="admin-detail-grid"><section className="admin-panel"><div className="admin-section-heading"><div><span className="admin-eyebrow">محتوى الباب</span><h2>الدروس</h2></div><button type="button" className="admin-primary" onClick={openCreate}><Plus size={16} /> إضافة درس</button></div><div className="admin-lesson-list">{lessons.length === 0 ? <div className="admin-empty-mini">لم تضف دروسًا لهذا الباب بعد.</div> : lessons.map((lesson, index) => <article className="admin-lesson-row" key={lesson._id}><span className="admin-lesson-number">{String(index + 1).padStart(2, '0')}</span><span className="admin-lesson-copy"><strong>{lesson.title}</strong><small><Clock3 size={12} /> {lesson.duration || 0} دقيقة · <Video size={12} /> {lesson.videoProvider === 'vimeo' ? 'Vimeo' : 'YouTube'}</small></span><span className="admin-lesson-actions"><button type="button" aria-label="تعديل الدرس" onClick={() => openEdit(lesson)}><Edit3 size={15} /></button><button type="button" aria-label="حذف الدرس" onClick={() => removeLesson(lesson)}><Trash2 size={15} /></button></span></article>)}</div></section><aside className="admin-panel admin-exam-panel"><div className="admin-section-heading"><div><span className="admin-eyebrow">اختبارات الفهم</span><h2>امتحانات الباب</h2></div><button type="button" className="admin-icon-button" onClick={() => navigate(`/exams/manage?courseId=${id}`)} aria-label="إضافة امتحان"><Plus size={16} /></button></div>{exams.length === 0 ? <div className="admin-empty-mini"><FileText size={20} /><p>لا يوجد امتحان لهذا الباب.</p><button type="button" className="admin-text-link" onClick={() => navigate(`/exams/manage?courseId=${id}`)}>إضافة امتحان <ChevronLeft size={14} /></button></div> : <div className="admin-exam-list">{exams.map((exam) => <button type="button" className="admin-exam-row" key={exam._id} onClick={() => navigate(`/admin/exams/${exam._id}`)}><span><FileText size={16} /></span><span><strong>{exam.title}</strong><small>{exam.timeLimitMin ? `${exam.timeLimitMin} دقيقة` : 'بدون وقت محدد'} · {exam.totalMarks || 0} درجات</small></span><ChevronLeft size={15} /></button>)}</div>}</aside></div><section className="admin-video-note"><span className="admin-note-icon"><Link2 size={18} /></span><div><strong>إضافة الفيديو</strong><p>الصق رابط المشاركة من YouTube أو Vimeo. المنصة توحّد الرابط وتعرض معاينة من داخل الدرس للطالب بعد التفعيل.</p></div><ExternalLink size={16} /></section>{dialog && <div className="admin-dialog-backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) setDialog(false); }}><div className="admin-dialog" role="dialog" aria-modal="true" aria-labelledby="lesson-dialog-title"><div className="admin-dialog-head"><div><span className="admin-eyebrow">{editing ? 'تعديل الدرس' : 'درس جديد'}</span><h2 id="lesson-dialog-title">{editing ? 'تعديل بيانات الدرس' : 'إضافة درس للباب'}</h2></div><button type="button" onClick={() => setDialog(false)} aria-label="إغلاق"><X size={18} /></button></div><form className="admin-form" onSubmit={submit}><label>عنوان الدرس<input value={form.title} onChange={(event) => setForm((value) => ({ ...value, title: event.target.value }))} placeholder="مثال: مفهوم الحركة" /></label><div className="admin-form-grid"><label>المدة بالدقائق<input type="number" min="0" value={form.duration} onChange={(event) => setForm((value) => ({ ...value, duration: event.target.value }))} /></label><label>ترتيب الدرس<input type="number" min="0" value={form.order} onChange={(event) => setForm((value) => ({ ...value, order: event.target.value }))} /></label></div><label>وصف مختصر<textarea value={form.description} onChange={(event) => setForm((value) => ({ ...value, description: event.target.value }))} rows={3} placeholder="ما الذي سيتعلمه الطالب؟" /></label><label>رابط الفيديو<input dir="ltr" value={form.videoUrl} onChange={(event) => setForm((value) => ({ ...value, videoUrl: event.target.value }))} placeholder="https://youtube.com/watch?v=... أو https://vimeo.com/..." /></label><div className="admin-url-hint"><Link2 size={15} /> يقبل YouTube وVimeo فقط</div><button type="submit" className="admin-primary full-admin-button" disabled={saving}>{saving ? <><span className="spinner spinner-light" /> جارٍ الحفظ...</> : <><Check size={16} /> حفظ الدرس</>}</button></form></div></div>}</div></AppShell>;
}
